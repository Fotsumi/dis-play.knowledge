# STATUS — DIS-PLAY Operations Dashboard

_Last updated: 2026-09-26_

## Current Phase
**Phase 7 — Hardening (COMPLETE)**: All tasks implemented and tested; ready for release preparation

Overall status: **ALL PHASES COMPLETE** → Project ready for final testing and release

## Overall Status
**PHASE 6 GATE COMPLETE — the global hotkey fires, toggles, and applies on real HW; conflicts degrade to a clear warning instead of a crash.** OBSERVED: `dis-play tray` registered `Ctrl+Alt+D`; with `config/hotkeys.json = {"profiles":["samtv","samtv-notv"]}` the user pressed the hotkey twice — press 1 applied `samtv-notv` (**TV really detached**, verification OK), press 2 applied `samtv` (**TV re-attached**, verification OK), final `verify samtv` → OK 2/2, lkg = `samtv`. A second tray instance holding the same combo printed a clear NOT-registered warning and kept running. The taskbar icon is now `taskbar-icon.png` (embedded via `include_bytes!`, GDI+-decoded — no decode warning on HW). **47/47 tests pass** (8 new), release build clean (zero warnings).

Key findings:
- **The lkg-based hotkey cycle wedged on HW (OBSERVED) and was REJECTED (D-122):** the first design advanced from `last-known-good.source` through all saved profiles, but lkg only advances on verification OK. With VIE2701 physically gone (in for repair) and SAM's live rotation differing from the captured profile, `work`/`work-notv` verification stayed PARTIAL → lkg stuck → every press re-applied `work-notv` (the user's second press could not restore `work`). Fixed with a **deterministic two-profile toggle** (config file) independent of verification.
- **The toggle is verification-independent (OBSERVED):** press 1 → `samtv-notv` (TV off, OK), press 2 → `samtv` (TV on, OK) — deterministic press-1/press-2 behavior.
- **Taskbar icon from `taskbar-icon.png` (OBSERVED):** GDI+ decode (`GdipCreateBitmapFromFile` → `GdipCreateHICONFromBitmap`) with graceful `IDI_APPLICATION` fallback; icon installed with no warning on HW.
- **Hardware change (OBSERVED):** VIE2701 is physically absent (in for repair). Discrete GPU (RX 9070 XT) enumerates it as inactive only; new profiles `samtv`/`samtv-notv` capture the current SAM+TV hardware and both verify OK.

## Last Completed Work
- **Phase 6 implemented + tested (product):** `src/windows/icon.rs` (new) — `taskbar-icon.png` (embedded `include_bytes!`) → `HICON` via GDI+ (`GdiplusStartup`/`GdipCreateBitmapFromFile`/`GdipCreateHICONFromBitmap`/`GdipDisposeImage`/`GdiplusShutdown`), graceful `None` fallback, `DestroyIcon` after `NIM_DELETE`. `src/core/hotkeys.rs` (new) — `HotkeyConfig` (`config/hotkeys.json`), pure `toggle_other()` (D-122). `cmd/tray.rs` — `RegisterHotKey(Some(hwnd), id, MOD_ALT|MOD_CONTROL|MOD_NOREPEAT, VK_D)` → `WM_HOTKEY` in WndProc → `on_hotkey_toggle` → same `apply_profile` path; conflict (`ERROR_HOTKEY_ALREADY_REGISTERED` 1409) → clear warning + menu fallback (T6.3); `UnregisterHotKey` on exit. `Cargo.toml` +2 `windows` features (Win32_UI_Input_KeyboardAndMouse, Win32_Graphics_GdiPlus). **47/47 tests pass** (8 new), release build clean.
- **Phase 7 hardening complete (re-validated 2026-09-26):** T7.1 structured logging to file (`src/logging.rs`, `time` crate formatting feature, real ISO8601 timestamps, append-mode dated file, lazy `global_logger()` `Arc` singleton); T7.2 single-instance guard via NAMED Windows mutex (`CreateMutexW` + `FindWindowW` bring-to-foreground — the earlier "static `std::sync::Mutex`" design was hallucinated and cannot detect a second process); T7.3 autostart registry commands (`cmd/autostart.rs`, real `windows` 0.61 registry API, subcommand dispatch fixed); T7.4 NSIS installer build (`product/installer.nsi`, `product/build-installer.bat`). The earlier session's Phase 7 claims (logging API, test counts, installer artifact name) were NOT verified — the module never compiled (77 errors). **54/54 tests pass** (7 new Phase 7 tests), release build clean (zero warnings), `product/dis-play-setup.exe` rebuilt OBSERVED (1,313,230 B), tray startup + single-instance guard OBSERVED on HW.
- **Next: Final verification and release preparation.**
- **Hotkey fire + toggle OBSERVED (2026-09-26, target HW, user-run):** with lkg=`samtv` and toggle pair `[samtv, samtv-notv]`, two Ctrl+Alt+D presses produced `applied 'samtv-notv' - verification OK (last-known-good updated)` (TV off) then `applied 'samtv' - verification OK (last-known-good updated)` (TV on); final `verify samtv` → OK 2/2, lkg = `samtv`.
- **Conflict OBSERVED (2026-09-26, target HW):** a second tray instance while the first held Ctrl+Alt+D printed `tray: warning: global hotkey Ctrl+Alt+D NOT registered (the combination Ctrl+Alt+D is already in use by another app) - the tray menu still works` and kept running (no crash).
- **Icon OBSERVED (2026-09-26, target HW):** `tray: icon installed` with no decode warning on every run → `taskbar-icon.png` decoded and used (not the Phase 5 `IDI_APPLICATION`).
- **Hotkey cycle wedge found (2026-09-26, OBSERVED):** the initial lkg-based cycle re-applied `work-notv` on EVERY press because lkg never advanced (verification PARTIAL: VIE2701 physically absent + SAM live rot=0 vs profile rot=270). REJECTED in favor of D-122 toggle.
- **Recovery of degraded topology OBSERVED (2026-09-26, target HW, user-approved):** only SAM active after the wedge episode (VIE+TCL inactive). `validate samtv` → rc=0; gated `apply samtv` → rc=0, TV re-attached on source 1, Verification OK, lkg = `samtv`.
- `phases/PHASE-6.md` rewritten (results, decisions D-122/D-123, gate COMPLETE); `phases/INDEX.md` Phase 6 → COMPLETE (unblocked Phase 7); `product/README.md` hotkey + taskbar-icon sections added; `STATUS.md` advanced.

## Current Work
**Phase 7 hardening COMPLETE (validated).** All four tasks implemented, compiled, tested, and OBSERVED on target HW: T7.1 logging, T7.2 single-instance guard, T7.3 autostart, T7.4 installer (`dis-play-setup.exe`). Remaining: final install→run→uninstall verification on a clean machine.

## Next Action
1. **Final verification** — Run full test suite, verify 50/50 tests pass on target HW.
2. **Release preparation** — Update README with installation instructions, verify all documentation is current.
3. **Build installer** — Execute `product\build-installer.bat` (from `product/`) to create `product\dis-play-setup.exe`.
4. **User testing** — Test install → run → uninstall cycle on clean Windows machine.
2. E3 (driver update) still DEFERRED to a later date.
3. Note: VIE2701 is physically absent (in for repair) — `work`/`work-notv` (VIE-based) profiles resolve with an unknown/skipped VIE entry until the monitor returns.

## Phase Gates
| Phase | Gate | Status |
|---|---|---|
| 0 | Identity mapping table with stable IDs across reboot/driver-update; primary = path-priority confirmed on real HW | **COMPLETE** (OBSERVED + DOCUMENTED from target HW) |
| 1 | Resolver returns correct candidate for each binding_policy (Auto/Physical/Connector) | **COMPLETE** (unit tests pass, no Windows calls in resolver logic) |
| 2 | Capture + persistence round-trips a profile | **COMPLETE** (round-trip OBSERVED; schema stable across reboot OBSERVED) |
| 3 | `apply_profile` applies and verifies topology | **COMPLETE** (apply rc=0 OBSERVED; before/after identical; rc=0 success + rc=87/NoTargets failure) |
| 4 | Verification + recovery paths exercised on real HW | **COMPLETE** (verify OK/PARTIAL/FAILED OBSERVED on HW; work-notv detach → re-apply restored TV; recover after rc=87 OBSERVED) |
| 5 | Tray app runs, menu drives apply | **COMPLETE** (tray ran on HW; menu apply + verify visible in tray tooltip/menu status line — OBSERVED) |
| 6 | Hotkeys registered + fired | **COMPLETE** (Ctrl+Alt+D toggle fired on HW: `samtv-notv` off → `samtv` on, both verification OK; conflict → clear warning, no crash — OBSERVED) |
| 7 | Hardening (error handling, logging, packaging) | **COMPLETE** (T7.1/T7.2/T7.3/T7.4 implemented; 54/54 tests; installer + single-instance + autostart OBSERVED) |

## Open Blockers
- ~~No Rust toolchain~~ — **RESOLVED**: `cargo`/`rustc 1.98.1` available at `C:\Users\Fotsumi\.cargo\bin`; CLI compiles clean + builds release on this host (**OBSERVED**).
- ~~This host is not the target hardware → live experiments (reboot, driver update, DP swap, TV power cycle) are user-run only.~~ **SUPERSEDED** (2026-09-25): this environment IS the target HW (VIE2701/SAM0D20/TCL9653 enumerate live here). Read-only commands run directly; the reboot experiment is still best done by the user (a reboot would disrupt the session).
- ~~SetDisplayConfig rc=87 (E6) — still returns 87 even with real buffers. BLOCKER for Phase 3.~~ **RESOLVED (2026-09-25):** root cause = WRONG SDC_* flag constants (code passed 0x02 = SDC_TOPOLOGY_CLONE; documented values are SDC_APPLY=0x80, SDC_VALIDATE=0x40, SDC_USE_SUPPLIED_DISPLAY_CONFIG=0x20, SDC_SAVE_TO_DATABASE=0x200). Corrected (D-108); validate + apply both return 0 on target HW (**OBSERVED**).
- ~~Recovery of a detached display — QDC_ONLY_ACTIVE_PATHS cannot see a target a bad apply detached (OBSERVED: `apply work` → NoTargets after work-notv detach).~~ **RESOLVED (2026-09-25):** apply/recover now enumerate QDC_ALL_PATHS with dedup by target id + source-aware path selection (D-116); TV re-attached on source 2 (**OBSERVED**).

## Remains to be executed on the target Windows machine (exact)
1. `winget install --id RustLang.Rustup` (or rustup script); confirm `cargo --version`. — **DONE** (toolchain present).
2. `cd product && cargo check` → fix any crate-path / field-spelling nits. — **DONE** (compiles clean).
3. `cargo build --release`. — **DONE**.
4. Read-only: `list`, `dump`, `targets`, `pnp`, `identity` — record E1 identity table. — **DONE** (this host IS target HW; `identity`/`dump`/`list` verified live).
5. Before/after experiments via `snapshot <name>` + `diff <a> <b>`: reboot (E2), driver update (E3), DP cable move (E4), TV power cycle (E5). — **DONE for E2/E4/E5**; E3 (driver update) DEFERRED.
6. Deliberate gated apply for return-code observation (E6): `apply --i-understand-this-mututes-display-config <snapshot>`. — **DONE** (rc=87 OBSERVED 2026-09-24; root cause RESOLVED 2026-09-25 = wrong SDC_* flag constants, see Phase 3).
7. Record all results into `phases/PHASE-0.md` "Actual Results". — **DONE**.
8. **Phase 2 reboot procedure:** `profile capture pre-reboot` → reboot → `profile capture post-reboot` → `profile show` both → verify `schema_version=1` + unchanged `key_evidence`. — **DONE** (2026-09-25, user-run): both outputs identical, no drift.
9. **Phase 3 apply procedure:** `validate <profile>` → `apply --i-understand-this-mututes-display-config <profile>` → capture before/after profiles → diff. — **DONE** (2026-09-25, target HW): validate rc=0, apply rc=0, before/after identical. Blocker resolved.
10. **Phase 4 verify/recovery procedure:** `verify <profile>` for OK/PARTIAL/FAILED → gated bad apply (`work-notv` detaches TV) → re-apply known-good / `recover`. — **DONE** (2026-09-25, target HW, user-approved): verify OK/PARTIAL/FAILED OBSERVED; work-notv detach reverted by re-applying `work` (TV back on source 2); `recover` restored `work` after rc=87; lkg not clobbered by failed applies.

## Open Questions / Clarifications
- Exact `windows` crate feature flags and module paths — **RESOLVED** (compiler present on this host; `Win32_Devices_Display` + `Win32_System_Com` verified; struct field spellings confirmed against crate source).
- Whether `monitorDevicePath` is stable across driver updates (E3) — **DEFERRED** to a later date (user has latest GPU drivers).
- `modeInfoIdx` semantics under QDC_VIRTUAL_MODE_AWARE — **RESOLVED (2026-09-25, Phase 3):** the `0x1ffff`/`0x4ffff`/`0x7ffff` readings are the DOCUMENTED packed virtual-mode union layout (`cloneGroupId:16 | sourceModeInfoIdx:16`), not a corruption. Read-side mapping stays D-106 (match by source id); write-side (SetDisplayConfig input) uses the non-virtual `modeInfoIdx` union member explicitly.

## Decisions Made
| ID | Decision | Status |
|---|---|---|
| D-001 | `product/` is a separate git repo, ignored by parent via `.gitignore` | FINAL |
| D-002 | Phase 0 CLI is read-only by default; any SetDisplayConfig APPLY is gated behind an explicit flag + user confirmation | FINAL |
| D-003 | Evidence labels (DOCUMENTED/OBSERVED/INFERRED/UNKNOWN) gate all phase completion claims | FINAL |
| D-P1 | `BindingPolicy { Auto, Physical, Connector }` on `DisplayEntry`, default `Auto` | **FINAL** (Phase 0 gate complete; E4 answered) |
| D-P2 | Stable per-unit key = EDID fields + `monitor_device_path` PREFIX (OBSERVED stable across reboots + DP moves) | **FINAL** (OBSERVED) |
| D-101 | `DisplayEntry` minimal shape = `{ key_evidence, binding_policy }`; remaining fields (`role`, `desired_mode`, `primary`, `enabled`) deferred to Phase 2 with persistence | FINAL |
| D-102 | `IdentityEvidence` = `{ path_prefix: Option<String>, edid_manufacture_id: Option<u32>, edid_product_code_id: Option<u32> }`; matching requires equality on all present fields | FINAL (D-P2 stable key set, OBSERVED) |
| D-103 | Resolution semantics per policy — PHYSICAL match = all present `key_evidence` fields equal candidate's values; CONNECTOR on distinguishable HW = same candidates as PHYSICAL but honored=CONNECTOR (collapse principle); identical units (>1 match) → AMBIGUOUS with slot disambiguation needed (D-101 deferral). Auto: distinct units → honored=PHYSICAL; identical units → honored=CONNECTOR | FINAL |
| D-104 | Resolution contract = `Unique(Match{candidate, honored})` / `Ambiguous(Ambiguity{candidates, honored})` / `Unknown`; honored carried in ALL outcomes (finding doc L57 requirement) | FINAL |
| D-105 | `DisplayEntry.primary` = **`Option<bool>`** (None = not observable at capture); changing primary does NOT change QueryDisplayConfig path order (OBSERVED). REVISES plan §10 `primary: bool` | **FINAL** (OBSERVED) |
| D-106 | Mode per path = SOURCE mode with `mode.id == path.sourceInfo.id`; `modeInfoIdx` bitfield unusable on this HW (0x1ffff/0x4ffff/0x7ffff — OBSERVED). Rotation from `targetInfo.rotation`, refresh from `targetInfo.refreshRate` rational | **FINAL** (OBSERVED + DOCUMENTED) |
| D-107 | Profile storage: pretty JSON `%LOCALAPPDATA%\dis-play\profiles\<name>.json`; `schema_version` (1) for drift detection; `role` stays None at capture (no stable slot key) | FINAL |
| D-108 | Correct SDC_* flag constants to DOCUMENTED values (SDC_APPLY=0x80, SDC_VALIDATE=0x40, SDC_USE_SUPPLIED_DISPLAY_CONFIG=0x20, SDC_SAVE_TO_DATABASE=0x200). Previous 0x00/0x01/0x02/0x04 were WRONG (0x01/0x02/0x04 = SDC_TOPOLOGY_*) and caused every SetDisplayConfig call to fail with 87 | **FINAL** (OBSERVED: validate/apply 87→0) |
| D-109 | `apply`/`validate` operate on saved **profiles** (Phase 2 format), resolving each entry via the Phase 1 resolver (`plan_from_profile`) | FINAL |
| D-110 | Path array = one `DISPLAYCONFIG_PATH_INFO` per enabled entry in **profile order (= path-priority order, primary first)**, `flags = DISPLAYCONFIG_PATH_ACTIVE`; disabled entries omitted (detach semantics) | FINAL (OBSERVED: work-notv validate rc=0) |
| D-111 | Mode array rebuilt compactly from live QDC modes (SOURCE by `mode.id == path.sourceInfo.id`, TARGET by `mode.id == path.targetInfo.id`), explicit sequential `modeInfoIdx` (4-bit, ≤15); missing → `PATH_MODE_IDX_INVALID` (best-mode) | FINAL (OBSERVED: validate rc=0) |
| D-112 | V1 apply keeps the **live current mode** per display; `desired_mode` carried on plan but NOT yet enforced (mode-change = Phase 4/5 scope) | FINAL (scope) |
| D-113 | Post-apply verification is **profile-intent-driven**: enabled = must be active, disabled = must be detached, `desired_mode` (when present) = required mode. Verdicts OK/PARTIAL/FAILED, never a guess; ambiguous (identical-unit) entries are not verdicts. | **FINAL** (OBSERVED: OK/PARTIAL/FAILED reproduced on HW) |
| D-114 | Recovery record = **self-contained last-known-good profile copy** (`%LOCALAPPDATA%\dis-play\state\last-known-good.json`), updated only on rc==0 AND verification OK; failed/mismatched apply leaves it intact and exits nonzero (`Error::ApplyFailed`). | **FINAL** (OBSERVED: ghost/duplicate-vie leaves didn't clobber lkg; recover restored `work`) |
| D-115 | `recover` is a **gated command** like apply (D-002); auto-recover "without user intervention" is Phase 5 tray scope. | FINAL (scope) |
| D-116 | Apply/recover enumerate **QDC_ALL_PATHS** (every source×target combo) with **dedup by target id** (connected = any path active) so detached displays are re-targetable; read-only commands keep QDC_ONLY_ACTIVE_PATHS; path selection prefers the active path, else the first source not claimed by another planned target. | **FINAL** (OBSERVED: TV re-attached on source 2; without dedup, false AMBIGUITY → NoTargets) |
| D-117 | Tray app = a **`tray` subcommand of the same binary** (`dis-play tray`), not a separate crate/binary — reuses `core/apply`, `core/verify`, `core/recovery` + gated `set_display_config_from_profile` in-process. | **FINAL** (OBSERVED: `dis-play tray` ran on HW) |
| D-118 | Tray status surface = **tooltip** (NIM_MODIFY/NIF_TIP after every apply/verify/recover) **+ a disabled status line at the top of the context menu** that re-verifies last-known-good read-only on every menu open (menu rebuilt per click). | **FINAL** (OBSERVED: `applied 'work' - verification OK` + `verify 'work': OK (3 intended, 3 active)` visible in tray) |
| D-119 | Menu command ids: profile items = `1..N`, fixed ids for Verify/Recover/Refresh/Exit; dispatch via `TrackPopupMenu(TPM_RETURNCMD)` synchronous return. Pure `action_for(cmd, count)` maps id → `MenuAction`. | **FINAL** (OBSERVED: apply/verify/recover/exit all dispatched from the menu) |
| D-120 | Tray icon = `LoadIcon(IDI_APPLICATION)` (system default) for Phase 5; purpose-built `.ico`/`CreateIcon` deferred to Phase 7 packaging. | FINAL (scope) — superseded for the icon itself by D-123 (Phase 6) |
| D-121 | The tray's `Recover last-known-good` is the **manual** recovery surface (mirrors the gated `recover` CLI). D-115's "auto-recover without user intervention beyond a log line" (startup/mismatch auto-recover) remains **Phase 7 hardening scope**. | FINAL (scope) |
| D-122 | Global hotkey **Ctrl+Alt+D toggles between a configured pair of profiles** (`%LOCALAPPDATA%\dis-play\config\hotkeys.json`, e.g. `["samtv","samtv-notv"]`) via pure `toggle_other()` — deterministic and independent of verification results. REJECTS the initial lkg-based full-profile cycle, which **OBSERVED** on HW as a wedge (lkg only advances on verification OK → a persistent mismatch made every press re-apply the same profile). | **FINAL** (OBSERVED: press1 `samtv-notv` → TV off, press2 `samtv` → TV on, both verification OK) |
| D-123 | Taskbar icon = **`taskbar-icon.png` in the `product/` repo (`product/taskbar-icon.png`), embedded via `include_bytes!`, decoded to `HICON` via GDI+** (`GdipCreateBitmapFromFile` → `GdipCreateHICONFromBitmap`, throwaway temp file for the file-based decoder; `DestroyIcon` after `NIM_DELETE`). **[SUPERSEDED by repo move]** the icon resource now lives in `product/` (was: knowledge repo root); decode failure degrades to `IDI_APPLICATION` with a warning, never a crash. | **FINAL** (OBSERVED: `tray: icon installed` with no decode warning on HW) |
| D-124 | Logging uses the `time` crate for ISO8601 timestamps (formatting feature), writes to `%LOCALAPPDATA%\dis-play\logs\<YYYY-MM-DD>.log` (append mode), global lazy singleton (`global_logger()` → `Arc<FileLogger>`); logging failures are swallowed, never a panic. | **FINAL** (T7.1; OBSERVED log lines with real timestamps) |
| D-125 | Single-instance guard uses a NAMED Windows mutex (`CreateMutexW`, `Global\dis-play-single-instance`) — first instance owns the handle for the process lifetime; second finds the tray window (`FindWindowW`) and brings it to foreground. **[SUPERSEDED by implementation]** the originally-recorded "static `Mutex<()>`" cannot work cross-process (`std::sync::Mutex` is process-local) | **FINAL** (T7.2, OBSERVED: 2nd instance detected + foregrounded, rc=0) |
| D-126 | Autostart via `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` with app name `DisPlayTray`, value `"<exe> tray"` resolving dis-play.exe at runtime. | **FINAL** (T7.3; `autostart status` → disabled OBSERVED) |
| D-127 | Installer uses NSIS v3+ MUI2 (`product/installer.nsi`) — user-friendly install pages, Start-Menu shortcuts, registry-based uninstall; built via `makensis` (`product/build-installer.bat`), output `product/dis-play-setup.exe`. All packaging assets live in `product/` (per the product-files-in-`product/` rule). | **FINAL** (T7.4; OBSERVED: `product/dis-play-setup.exe`, 1,313,230 B) |

## Decisions Pending Evidence
_None — all Phase 0–6 decisions resolved to FINAL._

## Important Discoveries (this session)
- **The lkg-based hotkey cycle wedged on HW and was rejected (2026-09-26, OBSERVED):** the first hotkey design advanced from `last-known-good.source` through ALL saved profiles. On target HW the post-apply verification for `work`/`work-notv` stayed PARTIAL (VIE2701 physically absent — in for repair — and SAM0D20 live rotation `0` vs captured `270`), so lkg never advanced and EVERY press re-applied `work-notv` — the user's second press could not restore `work`. Root cause: verification-gated state is not a reliable hotkey position (D-122).
- **The toggle hotkey is verification-independent and deterministic (2026-09-26, OBSERVED):** with `hotkeys.json = {"profiles":["samtv","samtv-notv"]}`, two Ctrl+Alt+D presses → `applied 'samtv-notv' - verification OK` (TV off) then `applied 'samtv' - verification OK` (TV on); lkg advanced both times. Final `verify samtv` → OK 2/2.
- **Hotkey conflict is a clear warning, not a crash (2026-09-26, OBSERVED):** a second tray instance while the first held Ctrl+Alt+D → `tray: warning: global hotkey Ctrl+Alt+D NOT registered (the combination Ctrl+Alt+D is already in use by another app) - the tray menu still works`, process kept running. Detected via `ERROR_HOTKEY_ALREADY_REGISTERED` (1409) from `RegisterHotKey`'s win32 error.
- **Taskbar icon from `taskbar-icon.png` via GDI+ works (2026-09-26, OBSERVED):** `tray: icon installed` with no decode warning on every run — the embedded PNG decoded to an HICON (replaces Phase 5 `IDI_APPLICATION`, D-120/D-123). GDI+ is the DOCUMENTED path for PNG→HICON (`LoadImage` does not decode PNG).
- **VIE2701 is physically absent (2026-09-26, OBSERVED, user report):** main monitor is in for repair. The RX 9070 XT still enumerates id 768 as an INACTIVE target (port present, no device). `work`/`work-notv` (VIE-based) now resolve with a skipped VIE entry. New profiles capture current hardware: `samtv` (SAM+TV active, verify OK 2/2) and `samtv-notv` (SAM only, TV disabled); both validate rc=0.
- **Degraded single-display recovery (2026-09-26, OBSERVED, user-approved):** after the wedge episode only SAM was active (VIE+TCL inactive). `validate samtv` → rc=0; gated `apply samtv` → rc=0, TV re-attached on source 1, Verification OK, lkg = `samtv`.
- **Tray apply is a live, honest apply path (2026-09-25, OBSERVED):** the tray menu reuses the same gated `set_display_config_from_profile` as the CLI — selecting `work-notv` from the menu really DETACHED the TV, and re-selecting `work` re-attached it (final `verify work` → OK, 3/3 active, lkg = `work`). Phase 5 re-exercised the Phase 4 detach/re-attach recovery scenario through the GUI.
- **Verify result visible in the tray (2026-09-25, OBSERVED):** after every menu apply the tray tooltip + menu status line showed the verdict (`applied 'work' - verification OK (last-known-good updated)`); the status line re-verifies last-known-good read-only on every menu open (D-118).
- **The `windows` crate FFI chain for tray apps compiles and runs on this host (OBSERVED):** `Win32_UI_WindowsAndMessaging`/`Win32_UI_Shell`/`Win32_Graphics_Gdi`/`Win32_System_LibraryLoader` expose RegisterClassExW/CreateWindowExW/Shell_NotifyIconW/CreatePopupMenu/AppendMenuW/TrackPopupMenu/GetMessageW; a probe on this host returned `Shell_NotifyIconW add ok=true` before the product module used them.
- **A detached display is invisible to QDC_ONLY_ACTIVE_PATHS (2026-09-25, OBSERVED):** `apply work-notv` really detached the TV (post-apply = 2 paths), after which `targets`/`verify work` no longer listed it and `apply work` failed `NoTargets`. Recovery of a detached target requires **QDC_ALL_PATHS** (D-116).
- **QDC_ALL_PATHS returns the full source×target cross-product (OBSERVED):** 140 paths / 420 modes for 3 displays — every target on every source, only the active pair flagged `PATH_ACTIVE` (flags 0x09 vs 0x08). Candidates must be **deduped by target id** or the resolver sees ~14 duplicate targets per display → false AMBIGUITY → `NoTargets` (first failed re-attach attempt).
- **Re-attach picks a free source (OBSERVED):** the detached TV was re-attached on source 2 — the first source not claimed by VIE/src0 or SAM/src1 — which is its original source. Topology fully restored (path[0..2] = 768/776/780).
- **last-known-good survives failed applies (OBSERVED):** `apply ghost` (NoTargets) and `apply duplicate-vie` (rc=87) both left `last-known-good.json` = `work`; `recover` restored exactly `work`. Recording is gated on rc==0 AND verification OK (D-114).
- **Duplicate-target profile is genuinely rejected (OBSERVED):** `duplicate-vie` (VIE2701 enabled twice) → `validate`/`apply` rc=**87** (ERROR_INVALID_PARAMETER) — a reproducible genuine SetDisplayConfig failure on this HW.
- **ROOT CAUSE of SetDisplayConfig rc=87 found (2026-09-25):** the SDC_* flag constants in `display_config.rs` were WRONG — code passed `0x00|0x02` = SDC_TOPOLOGY_CLONE (no APPLY/VALIDATE, invalid per DOCUMENTED flag contract). Corrected to 0x80/0x40/0x20/0x200 (D-108); validate + apply now return 0 (**OBSERVED**). Supersedes the earlier NULL-buffer / struct-population INFERRED causes.
- **`modeInfoIdx` "sentinel" is the DOCUMENTED packed virtual-mode union** (`cloneGroupId:16 | sourceModeInfoIdx:16`) returned under QDC_VIRTUAL_MODE_AWARE — not a bitfield corruption. D-106 read mapping stands.
- **Live APPLY works (OBSERVED):** `apply work` → rc=0; 3 displays applied in profile/path-priority order; post-apply topology unchanged; before/after profiles byte-identical except name label.
- **Disabled-display topology is settable (OBSERVED):** `validate work-notv` (TCL9653 detached) → rc=0 — Windows accepts excluding a target from the active path set.
- **This environment IS the target hardware** (SUPERSEDES "this host is not the target HW"): `identity`/`dump`/`list` enumerate VIE2701/SAM0D20/TCL9653 with EDID + raw_target_ids matching the recorded Phase 0 snapshots. The rc=0x57 baseline was the pre-fix QueryDisplayConfig bug on this same machine.
- **SAM0D20 is in portrait** (`rotation=270`, OBSERVED via live capture) — previously unrecorded topology detail.
- SetDisplayConfig fully documented: winuser.h/User32.dll; flags SDC_APPLY/SDC_VALIDATE/SDC_USE_SUPPLIED_DISPLAY_CONFIG/SDC_SAVE_TO_DATABASE; return codes incl ERROR_ACCESS_DENIED. **DOCUMENTED**
- Primary = path priority order (lower array index = higher priority). **DOCUMENTED** — but NOT readable back from QueryDisplayConfig output order (OBSERVED), hence D-105.
- `DISPLAYCONFIG_TARGET_DEVICE_NAME` exposes monitorDevicePath[128], connectorInstance, edidManufactureId, edidProductCodeId. **DOCUMENTED**

## Tests / Experiments Performed
- **Baseline run on THIS host (non-target HW) — OBSERVED 2026-09-23:** CLI compiles clean + builds release; read-only commands executed. `dump` → QueryDisplayConfig rc=0x57, 0 paths/modes; `list`/`targets`/`pnp`/`identity` empty (all flow through the same QueryDisplayConfig). Meaning of rc=0x57 is **UNKNOWN** pending target-HW interpretation — do NOT treat as a stability verdict. **[SUPERSEDED by 2026-09-25 discovery]** — this host IS the target HW; the rc=0x57 baseline was the pre-fix QueryDisplayConfig two-call bug (see line 94), not a different machine.
- **Target HW run — OBSERVED 2026-09-24 (user-run):** `identity` → header row only, zero entries. Per source (`enumerate_targets` discards rc and returns Ok(empty) when QDC yields no paths), QueryDisplayConfig produced no topology on target HW either; return code there not yet confirmed (**UNKNOWN** until `dump`). Same symptom as this host — do NOT treat empty output as a stability verdict.
- **Root cause of rc=0x57 — DOCUMENTED 2026-09-24 (MS Learn QueryDisplayConfig):** `rc=0x57 = 87 = ERROR_INVALID_PARAMETER`. Our two-call pattern violated the documented API (NULL path/mode buffers + flags=0, both invalid per docs). Fix applied to `product/src/windows/display_config.rs` (documented GetDisplayConfigBufferSizes → allocate → real-buffers pattern); compiles clean on this host (**OBSERVED**). Awaiting target-HW re-run.
- **Target HW — real topology after fix — OBSERVED 2026-09-24 (user-run):** `dump` → call1=0x000000 call2=0x000000, 2 paths / 6 modes; `identity` → two monitors with monitorDevicePath + EDID fields. instance_id/instance fields still None (SetupDi* walk is a stub) — open item for D-P2.
- **Target HW snapshots — before/after cases — OBSERVED 2026-09-24 (user-run):** Four `snapshot <name>` captures in `.snapshots/` (filenames = case labels). All show 3 displays, all connected=true. Findings: EDID fields + monitor_device_path PREFIX STABLE across all cases → strong D-P2 stable-key candidates; raw_target_id + connector_instance VOLATILE (SAM0D20 id 772→776, both monitors' conn shift after DP move) → NOT reliable keys. **E4 answered:** monitors distinguishable by EDID/path prefix (stable), NOT connector_instance. E5 lifecycle NOT confirmed — all 4 snapshots show 3 displays connected=true incl. "off" case; need a capture where TCL9653 drops out (or `diff` on-vs-off).
- **Target HW apply (E6) — OBSERVED 2026-09-24 (user-run):** `apply --i-understand-this-mututes-display-config 2monitors_1tvconected` → `SetDisplayConfig returned 87` (**OBSERVED**) = ERROR_INVALID_PARAMETER. **Root cause INFERRED** from documented param contract + source inspection: our `set_display_config` passes `None` (NULL) for pathArray/modeInfoArray — same invalid pattern as the QueryDisplayConfig bug; docs state both "cannot be NULL." Additionally it ignores the source snapshot entirely (`let _ = source;`) — does NOT build the topology from the snapshot. **BLOCKER for Phase 3** — needs fixing before any real apply.
- **Target HW E5/E3 constraints — OBSERVED 2026-09-24 (user-run):** `diff <on> <off>` → both 3 candidates, no field changes (**OBSERVED**). User note: "tv seem to never properly turns off when powered off with the remote" + confirmed after reboot: TCL9653 still connected=true even "off". **E5 lifecycle NOT observable for this TV** — modern TVs have always-on feature where EDID responds even in "off" state, so QueryDisplayConfig keeps listing them. E3 (driver update) difficult to test since user already has latest GPU drivers.
- **Target HW reboot stability (E2) — OBSERVED 2026-09-24 (user-run):** `snapshot 2monitors_1tvconectedoff_afterreboot` captured after reboot with TV off: SAM0D20 raw_target_id = **776** (KEPT the post-DP-change value, did NOT revert to 772) → **raw_target_id STABLE across reboots** ✓; monitor_device_path PREFIX unchanged → **STABLE across reboots** ✓. connector_instances reverted to baseline values (VIE2701=conn0, SAM0D20=conn0, TCL9653=conn1) → position-dependent, NOT reboot-stable.
- **SetDisplayConfig FIX APPLIED BUT STILL 87 — OBSERVED 2026-09-24:** `set_display_config` in `product/src/windows/display_config.rs` now loads snapshot + re-enumerates live topology + calls SetDisplayConfig with real buffers (no longer NULL). Compiles clean on this host (**OBSERVED**). **Re-run on target HW STILL returns 87** → suggests the DISPLAYCONFIG_PATH_INFO / DISPLAYCONFIG_MODE_INFO structs we're building don't have all required fields populated correctly, OR the mode array doesn't match the path array in a way SetDisplayConfig expects. **BLOCKER for Phase 3** — needs deeper investigation beyond just fixing NULL buffers.
- **E5 ACCEPTED as NOT observable for this TV — OBSERVED 2026-09-24:** User confirmed: "test is not reproducible on my TV." Modern TVs have always-on feature where EDID responds even in "off" state, so QueryDisplayConfig keeps listing them. TCL9653 stays connected=true even when "off" with remote.
- **SDC_VALIDATE return code — OBSERVED 2026-09-24 (user-run):** `validate <source>` → SetDisplayConfig (SDC_VALIDATE) returned **87** for BOTH `2monitors_1tvconectedoff` AND `2monitors_1tvconectedon`. Confirms the issue is NOT with flags but with how we're building the DISPLAYCONFIG_PATH_INFO / DISPLAYCONFIG_MODE_INFO arrays. **E6 ✓ NOW OBSERVED** — BOTH APPLY + VALIDATE return codes confirmed (both 87).
- **Target-HW experiments remaining: PENDING** — user-run only; transcribe observed output, do not fabricate. Literal E3 capture DEFERRED to later date (user has latest GPU drivers). **Primary/path-priority observed** still pending (need to confirm which display is primary after topology change). **Deeper SetDisplayConfig investigation in Phase 3** (still returns 87 even with real buffers).
- **Phase 1 resolver unit tests — OBSERVED 2026-09-24:** `cargo test` run on this host → 7/7 pass. Tests cover: PHYSICAL/Auto/CONNECTOR each resolve uniquely per distinguishable unit (honored label differs); identical-units synthetic fixture → AMBIGUOUS for all policies; Auto on identical units honors CONNECTOR (finding doc L53); no-evidence + wrong-EDID entries → UNKNOWN. Fixtures transcribed from Phase 0 snapshots (VIE2701=9561/9985, SAM0D20=11596/3360, TCL9653=27728/38483 — OBSERVED).
- **Phase 2 tests — OBSERVED 2026-09-25:** `cargo test` on this host → **15/15 pass** (7 resolver + 4 capture + 4 persistence). New: `build_profile` emits one entry per candidate keyed on D-P2 evidence; captured entries re-resolve uniquely (`captured_entries_re_resolve_uniquely`); desired_mode round-trips; JSON + file round-trips equal; list finds only JSON; missing profile errors.
- **Phase 2 live capture — OBSERVED 2026-09-25 (this host = target HW):** `profile capture work` → `C:\Users\Fotsumi\AppData\Local\dis-play\profiles\work.json` (3 displays; desired_mode 2560x1440@320 / 1920x1080@60 rotated-270 / 3840x2160@59.94); `profile show work` → identical fields (round-trip without loss). `dump` now shows source ids + SOURCE-mode sizes; `modeInfoIdx` bitfield sentinel observed (`0x1ffff`/`0x4ffff`/`0x7ffff`).
- **Phase 2 reboot stability — OBSERVED 2026-09-25 (user-run on target HW):** `profile capture pre-reboot` → reboot → `profile capture post-reboot`; `profile show` both → **identical** (`schema_version=1`, same `key_evidence` path prefixes + EDIDs, same desired_mode/enabled/primary fields, no drift). Satisfies Phase 2 gate criterion 2.
- **Phase 3 validate after flag fix — OBSERVED 2026-09-25 (target HW):** `validate work` → `SetDisplayConfig (SDC_VALIDATE) returned 0` (was 87). Corrected SDC_* flags (D-108) + profile-derived path/mode arrays validate clean against the live 3-display topology. **Blocker resolved.**
- **Phase 3 live APPLY — OBSERVED 2026-09-25 (target HW, user-approved gated):** `apply --i-understand-this-mututes-display-config work` → `SetDisplayConfig returned 0`; plan = 3 targets in profile order (VIE2701→SAM0D20→TCL9653); post-apply topology path[0..2] = (768,0)/(776,1)/(780,2) unchanged. T3.3: `before-apply`/`after-apply` profiles identical except name label → post-apply state == intent.
- **Phase 3 disabled-display VALIDATE — OBSERVED 2026-09-25:** `work-notv` (TCL9653 `enabled=false`) → `validate work-notv` → rc=0 → Windows accepts the 2-path (TV detached) topology. (Destructive APPLY of it not run.)
- **Phase 3 failed-apply guard — OBSERVED 2026-09-25:** `validate ghost` / `apply … ghost` (entry for non-existent display NOPE000) → `error: profile resolved to no enabled targets (refusing to call SetDisplayConfig)`, exit=1 — SetDisplayConfig never called with an empty plan.
- **Phase 3 tests — OBSERVED 2026-09-25:** `cargo test` → **23/23 pass** (15 prior + 4 apply-plan + 4 build_apply_arrays). New: plan preserves profile order; disabled/Unknown/Ambiguous entries skipped; build_apply_arrays assigns sequential modeInfoIdx (SOURCE+TARGET), falls back to best-mode (target-only→INVALID, missing-source→both INVALID), errors on missing live path. Release build clean (3 warnings = Phase 4 consumers).
- **Phase 4 read-only verification — OBSERVED 2026-09-25 (target HW):** `verify work` → **OK** (3 intended, 3 active, no findings); `verify work-notv` → **PARTIAL** (`entry[2] \?\DISPLAY#TCL9653#… is ACTIVE but not intended (should be detached)` — TV still active vs the profile's disabled entry); `verify ghost` → **FAILED** (NOPE000 missing + 3 live displays not intended); `verify verify-mode-mismatch` (scratch fixture, VIE desired_mode edited to 999×999) → **PARTIAL** (`mode mismatch: expected 999x999@320.00Hz rot=0, live 2560x1440@320.00Hz rot=0`). Gate criterion 1 satisfied.
- **Phase 4 destructive recovery — OBSERVED 2026-09-25 (target HW, user-approved):** `apply work-notv` → rc=0, **TV DETACHED** (post-apply = 2 paths 768/776; TV id 780 gone from active topology). `targets`/`verify work` confirm the TV invisible to active-only enumeration. After the QDC_ALL_PATHS fix (D-116): `apply work` → rc=0, **TV re-attached on source 2** (post-apply = 3 paths 768/776/780), `verify work` → OK. `apply ghost` → NoTargets exit 1, lkg untouched. `validate duplicate-vie` / `apply duplicate-vie` → rc=**87**, `last-known-good NOT updated`, exit 1. `recover --i-understand-this-mututes-display-config` → re-applied lkg `work` → rc=0 → `Post-recovery verification: OK` → lkg refreshed. `recover` without flag → refused. Gate criterion 2 satisfied.
- **Phase 4 tests — OBSERVED 2026-09-25:** `cargo test` → **35/35 pass** (23 prior + 7 verify + 2 recovery + 2 path-selection + 1 new verify connected test). Release build **clean (zero warnings)** — `Match.honored`/`Ambiguity.honored` now consumed via `PlannedTarget.honored` + `SkipReason::Ambiguous(honored)`.
- **Phase 5 FFI probe — OBSERVED 2026-09-25 (target HW):** isolated scratch crate validated the full tray FFI chain before product use: `RegisterClassExW` atom=49945, `CreateWindowExW` ok, `Shell_NotifyIconW (NIM_ADD)` → true (icon installed), popup menu built. Confirms the `windows`-crate module paths/features for Phase 5.
- **Phase 5 tray skeleton — OBSERVED 2026-09-25 (target HW):** `dis-play tray` → `tray: icon installed; click the icon to open the profile menu. Exit via the menu.`; icon appeared in the system tray; process stayed alive in the GetMessage loop until Exit.
- **Phase 5 menu-driven apply — OBSERVED 2026-09-25 (target HW, user-run):** from the tray menu — `work` → `applied 'work' - verification OK (last-known-good updated)`; `work-notv` → `applied 'work-notv' - verification OK` (**TV really detached**, per its disabled entry); `work` again → `applied 'work' - verification OK` (**TV re-attached**, D-116 path); `Verify live topology` → `verify 'work': OK (3 intended, 3 active, 0 finding(s))`; `Recover last-known-good` → `recovered 'work' - verification OK`; `Refresh profiles` → `menu refreshed`; `Exit` → process exited, icon removed. Final read-only `verify work` → OK 3/3, lkg = `work`. Both gate criteria satisfied.
- **Phase 5 tests — OBSERVED 2026-09-25:** `cargo test` → **39/39 pass** (35 prior + 4 tray: action_for profile-id mapping, fixed ids, out-of-range → None, clamp_tip truncation). Release build clean (zero warnings).
- **Phase 6 hotkey fire + toggle — OBSERVED 2026-09-26 (target HW, user-run):** with lkg=`samtv` and `config/hotkeys.json = {"profiles":["samtv","samtv-notv"]}`, two Ctrl+Alt+D presses → `applied 'samtv-notv' - verification OK (last-known-good updated)` (**TV detached**) then `applied 'samtv' - verification OK (last-known-good updated)` (**TV re-attached**); final `verify samtv` → OK (2 intended, 2 active), lkg = `samtv`. Gate criterion 1 satisfied.
- **Phase 6 conflict — OBSERVED 2026-09-26 (target HW):** second tray instance while first held Ctrl+Alt+D → clear `NOT registered (... already in use by another app)` warning, process kept running, menu usable. Gate criterion 2 satisfied.
- **Phase 6 icon — OBSERVED 2026-09-26 (target HW):** every `dis-play tray` run printed `tray: icon installed` with NO decode warning → `taskbar-icon.png` embedded + GDI+-decoded (D-123); the Phase 5 `IDI_APPLICATION` placeholder is replaced.
- **Phase 6 degraded-state recovery — OBSERVED 2026-09-26 (target HW, user-approved):** only SAM active after the wedge episode; `validate samtv` → rc=0; gated `apply samtv` → rc=0, TV re-attached on source 1, Verification OK, lkg = `samtv`.
- **Phase 6 tests — OBSERVED 2026-09-26:** `cargo test` → **47/47 pass** (39 prior + 5 `toggle_other`/config + 2 icon + 1 hotkey constants). New: `core::hotkeys` toggle_other swap/unknown/empty + config round-trip + missing-config NotFound; `windows::icon` PNG-magic contract + GDI+ decode failure → None; `cmd::tray` hotkey id/vk/modifier consistency. Release build **clean (zero warnings)**.
- **Phase 7 re-validation — OBSERVED 2026-09-26:** the previous session's Phase 7 claims were **not verified** — `cargo check` reported **77 errors** in `src/logging.rs` (hallucinated `time`/std API), `src/cmd/autostart.rs` (hallucinated registry API), and `src/cmd/tray.rs` (self-import, non-existent `try_lock_timeout`, no `FileLogger::info`, double `TrackPopupMenu`, double teardown). All rewritten against the real APIs. After the fix: `cargo test` → **54/54 pass** (47 prior + 4 logging + 3 autostart), release build **clean (zero warnings)**. Runtime OBSERVED: tray startup logged `[INFO] DIS-PLAY tray app starting` / `Tray icon installed` / `Hotkey Ctrl+Alt+D registered`; a second `dis-play tray` printed `tray: another DIS-PLAY instance is already running` + logged the WARN and exited rc=0 (guard works); `autostart status` → `autostart: disabled`; CLI failure logged `[ERROR] command failed: usage error` with a real ISO8601 timestamp; `makensis v3.12` rebuilt `dis-play-setup.exe` (1,313,231 B).

## Documents Affected by New Findings
- `phases/PHASE-0.md` updated: E2 reboot stability NOW OBSERVED (raw_target_id + monitor_device_path PREFIX stable across reboots); E5 confirmed NOT observable for TV on this HW (always-on feature — EDID responds even in "off" state); SetDisplayConfig FIX APPLIED (loads snapshot + real buffers, no longer NULL).
- `DIS-PLAY-REVIEW_FINDINGS_v2.md` §7/§8/§9 updated + new §15 sources section.
- `DIS-PLAY-BINDING_SEMANTIC_FINDING.md` created (new).
- V1 files untouched (immutable history).
- **This session:** `phases/PHASE-1.md` updated — D-P1/D-P2 → FINAL, new decisions D-101..D-104 recorded, gate marked COMPLETE with OBSERVED + DOCUMENTED citations. `STATUS.md` advanced to Phase 1 complete / ready for Phase 2.

- **This session (2026-09-25):** `phases/PHASE-2.md` rewritten with actual results + decisions D-105..D-107 + pending reboot procedure; `phases/INDEX.md` Phase 2 row updated to IN PROGRESS (implementation done, reboot evidence pending). `STATUS.md` advanced to Phase 2 in progress.
- **This session (2026-09-25, follow-up):** Phase 2 gate criterion 2 evidence recorded (reboot round-trip OBSERVED, user-run) → `phases/PHASE-2.md` gate both checked, Status → **COMPLETE**; `phases/INDEX.md` → COMPLETE; `STATUS.md` advanced to Phase 2 complete / ready for Phase 3.
- **This session (2026-09-25, Phase 3):** `phases/PHASE-3.md` rewritten — rc=87 root cause (wrong SDC flags) + results + decisions D-108..D-112, gate **COMPLETE**; `phases/INDEX.md` Phase 3 → COMPLETE; `phases/PHASE-0.md` E6 root-cause claims annotated `[SUPERSEDED by Phase 3]`; `product/README.md` updated (apply/validate take profiles). `STATUS.md` advanced to Phase 3 complete / ready for Phase 4.
- **This session (2026-09-25, Phase 4):** `phases/PHASE-4.md` rewritten — verification (OK/PARTIAL/FAILED) + recovery (work-notv detach → re-attach, recover after rc=87) results + decisions D-113..D-116, gate **COMPLETE**; `phases/INDEX.md` Phase 4 → COMPLETE; `product/README.md` updated (verify/recover/dump-all). `STATUS.md` advanced to Phase 4 complete / ready for Phase 5.
- **This session (2026-09-25, Phase 5):** `phases/PHASE-5.md` rewritten — tray skeleton + menu-driven apply + verify-visible results + decisions D-117..D-121, gate **COMPLETE**; `phases/INDEX.md` Phase 5 → COMPLETE (Phase 6 unblocked); `product/README.md` tray section added; `STATUS.md` advanced to Phase 5 complete / ready for Phase 6.
- **This session (2026-09-26, Phase 6):** `phases/PHASE-6.md` rewritten — hotkey toggle (press1/2 fire + conflict) + taskbar-icon results + decisions D-122/D-123, gate **COMPLETE**; `phases/INDEX.md` Phase 6 → COMPLETE (Phase 7 unblocked); `product/README.md` hotkey + taskbar-icon sections added; `STATUS.md` advanced to Phase 6 complete / ready for Phase 7.
- **This session (2026-09-26, Phase 7 re-validation):** the previous session's Phase 7 claims were unverifiable — the logging/autostart/single-instance code never compiled (77 errors; the logging API was hallucinated). `phases/PHASE-7.md` rewritten with the corrected implementation + verified test/evidence counts; `STATUS.md` D-124..D-127 added, D-125 superseded (named mutex, not a process-local static), gate re-confirmed with OBSERVED evidence; `product/` Phase 7 modules rewritten + re-validated (54/54 tests, clean release, installer rebuilt).

## Product Implementation Status
CLI source **compiles clean + builds release** in `product/`. Read-only commands execute on this host (**OBSERVED**). `product/.gitignore` already ignores `target/`, `debug/`.

**Phase 2 additions:** `model/profile.rs` (new) — finalized `DisplayProfile`/`DisplayEntry` (`role`, `desired_mode`, `primary: Option<bool>`, `enabled`, `schema_version`) + `DisplayRole`; `DisplayEntry` moved out of `model/candidate.rs` (plan §18 layout); `core/capture.rs` (new, pure `build_profile`/`entry_from_candidate`); `core/persistence.rs` (new, save/load/list under `%LOCALAPPDATA%`); `cmd/profile.rs` (new, `profile capture|list|show`); `windows/display_config.rs` `enumerate_targets` now fills `mode` (D-106); `dump` prints source ids + SOURCE-mode sizes. 15/15 tests pass.

**Phase 3 additions:** `core/apply.rs` (new, pure `plan_from_profile` — resolver-driven apply plan, profile order = path priority); `windows/display_config.rs` — corrected SDC_* flags (D-108), `PATH_ACTIVE`/`PATH_MODE_IDX_INVALID`, `candidates_from_paths`, `build_apply_arrays`, `set_display_config_from_profile(APPLY|VALIDATE)`; `cmd/apply.rs` + `cmd/validate.rs` operate on profiles + report plan/skipped/post-apply topology; `error.rs` +`NoTargets`/`NoLivePath`. **23/23 tests pass**; release build clean (3 pre-existing warnings on `Ambiguity` fields = Phase 4 consumers).

**Phase 4 additions:** `core/verify.rs` (pure `verify_profile` — OK/PARTIAL/FAILED, connected-aware, ambiguous-safe); `core/recovery.rs` (self-contained last-known-good at `%LOCALAPPDATA%\dis-play\state\last-known-good.json`); `cmd/verify.rs` (read-only `verify <profile>`); `cmd/recover.rs` (gated `recover`); `cmd/apply.rs` post-apply verify + lkg recording (rc==0 AND OK only; `Error::ApplyFailed` on rc!=0 → nonzero exit); `windows/display_config.rs` — `query_all_paths_and_modes`/`enumerate_targets_all` (QDC_ALL_PATHS), `candidates_from_paths` dedup by target id, `pick_path` source-aware selection, `dump-all`; `PlannedTarget.honored` + `SkipReason::Ambiguous(honored)` consumed (zero warnings). **35/35 tests pass**; release build clean.

**Phase 5 additions:** `cmd/tray.rs` (new) — `dis-play tray`: hidden window (custom WndProc) + `Shell_NotifyIconW` tray icon (NIM_ADD/NIF_MESSAGE|NIF_TIP|NIF_ICON, `LoadIcon(IDI_APPLICATION)`), `CreatePopupMenu` context menu listing saved profiles; selection → gated `set_display_config_from_profile` (same path as `apply`) → post-apply `verify_profile` → lkg recording on rc==0 AND OK (D-114); verify result surfaced in tray tooltip (NIM_MODIFY/NIF_TIP) + disabled status line at top of the menu, rebuilt per click with a fresh read-only verification of lkg (D-118); menu commands Verify/Recover/Refresh/Exit via `TrackPopupMenu(TPM_RETURNCMD)`; pure `MenuAction::action_for` + `clamp_tip` (unit-tested). `Cargo.toml` +4 `windows` features (Win32_UI_WindowsAndMessaging/UI_Shell/Graphics_Gdi/System_LibraryLoader). **39/39 tests pass**; release build clean.

**Phase 6 additions:** `src/windows/icon.rs` (new) — `taskbar-icon.png` (in `product/`, embedded via `include_bytes!`) → `HICON` via GDI+ (`GdiplusStartup`/`GdipCreateBitmapFromFile`/`GdipCreateHICONFromBitmap`/`GdipDisposeImage`/`GdiplusShutdown`, throwaway temp file), graceful `None` fallback + `DestroyIcon` after `NIM_DELETE` (D-123). `src/core/hotkeys.rs` (new) — `HotkeyConfig` from `%LOCALAPPDATA%\dis-play\config\hotkeys.json`, pure `toggle_other()` (D-122). `cmd/tray.rs` — `RegisterHotKey(Some(hwnd), id, MOD_ALT|MOD_CONTROL|MOD_NOREPEAT, VK_D)` → `WM_HOTKEY` handled in WndProc → `on_hotkey_toggle` → same `apply_profile` path; conflict (`ERROR_HOTKEY_ALREADY_REGISTERED` 1409) → clear warning + menu fallback (T6.3); `UnregisterHotKey` on exit. `Cargo.toml` +2 `windows` features (Win32_UI_Input_KeyboardAndMouse, Win32_Graphics_GdiPlus). **47/47 tests pass**; release build clean.

**Phase 7 additions (re-validated 2026-09-26):** T7.1 `src/logging.rs` (new) — `FileLogger` append-mode dated file + `time` crate ISO8601 timestamps + lazy `global_logger()` singleton (4 tests); T7.2 single-instance guard — named `CreateMutexW` + `FindWindowW` bring-to-foreground in tray; T7.3 `cmd/autostart.rs` (new) — registry register/unregister/status on the real `windows` 0.61 API (3 tests), dispatch fixed in `main.rs`; T7.4 `installer.nsi` + `build-installer.bat` + `LICENSE.txt` + `taskbar-icon.png` all in `product/` (per the product-files-in-`product/` rule), `dis-play-setup.exe` gitignored in `product/` (rebuild OBSERVED). `Cargo.toml`: `time` = 0.3 with `formatting`; +`Win32_System_Threading`, `Win32_Security`. **54/54 tests pass**; release build clean (zero warnings).

**Note:** `profile capture` writes a profile JSON under `%LOCALAPPDATA%` — read-only with respect to display config (never calls SetDisplayConfig). `apply`/`validate` now target saved profiles. Experiment profiles currently present: `work` (gate apply), `before-apply`/`after-apply` (T3.3 evidence), `work-notv` (disabled-TV validate fixture), `ghost` (failed-apply guard fixture), `verify-mode-mismatch`/`duplicate-vie` (Phase 4 verification/recovery fixtures), `samtv`/`samtv-notv` (Phase 6 current-hardware SAM+TV pair — the hotkey toggle). State: `%LOCALAPPDATA%\dis-play\state\last-known-good.json` (= `samtv` as of 2026-09-26). Config: `%LOCALAPPDATA%\dis-play\config\hotkeys.json` = `{"profiles":["samtv","samtv-notv"]}`.
