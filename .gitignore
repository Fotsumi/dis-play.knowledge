# product/ is a separate Git repository (the implementation repo).
# It must stay out of the dis-play.knowledge knowledge/operations repository.
product/

# NSIS build output (regenerated by build-installer.bat — not knowledge).
dis-play-setup.exe
